Johnny
alarm_get(numb)|numb - alarm index#Similar to Studio function. Gets the value of an instance alarm.#
alarm_set(numb,steps)|numb - alarm index#Similar to Studio function. Sets the value of an instance alarm.#
angle_difference_3d(x1,y1,z1,x2,y2,z2)|
animation_stop()|Stops the instance's animation on the last frame.#Particularly useful for Animation end events.#
base64_decode(b64)|Returns a string of raw bytes, 8 bits each. b64 strings with#characters outside of the RFC 3548 standard or with excess#padding characters at the end will not decode correctly.#b64         base64 digits (RFC 3548), 6 bits each, string#/ GMLscripts.com/license#
base64_encode(str)|Returns a string of base64 digits (RFC 3548), 6 bits each.#str         raw bytes, 8 bits each, string#/ GMLscripts.com/license#
collision_check_fast(obj)|
color_blend(col1,col2)|
color_get_luminance(color)|
date_get_timestamp([date])|
direction_to_object(obj)|
directory_previous(dir)|
distance_to_instance(inst)|
distance_to_path(x,y,path)|
dot_product_3d_normalised(x1,y1,z1,x2,y2,z2)|
dot_product_normalised(x1,y1,x2,y2)|
draw_background_tiled_extra(back,x,y,xscale,yscale,angle,color,alpha,hrepeats,vrepeats)|
draw_reset()|
draw_self_as(sprite,[image])|
draw_self_floored()|
draw_set1(color,alpha)|
draw_set2(halign,valign)|
draw_set_rgba(r,g,b,a)|
draw_sprite_ext_fixed(sprite,image,x,y,xscale,yscale,angle,color,alpha)|
ds_list_equal(list1,list2)|
ds_map_add_copy(src,dest)|copies all keys from src to dest without clearing dest#
ds_map_get(map,key)|
ds_map_read_ini(map,filename)|
ds_map_read_ini_string(map,string)|
ds_map_set(map,key,value)|
dslist() -> list|
dslist(list) -> string|
dslist(list,pos) -> value|
dslist(list,pos,val) -> value|
dsmap() -> map|
dsmap(map) -> string|
dsmap(map,key) -> value|
dsmap(map,key,value) -> value|
event_alarm(numb)|numb - alarm index#Shortcut function. Executes the actions in the Alarm event indicated.#
event_beginstep()|Shortcut function. Executes the actions in the Begin Step event.#
event_draw()|Shortcut function. Executes the actions in the Draw event.#
event_endstep()|Shortcut function. Executes the actions in the End Step event.#
event_inherit_object(object)|
event_step()|Shortcut function. Executes the actions in the Step event.#
event_trigger(trig)|
execute_program_async(cmdline)|
execute_program_async_result()|
execute_program_silent(cmdline)|
file_text_read_all(filename,[line separator])|
filename_remove_ext(fn)|
font_add_file(filename,fontname,size,bold,italic,first,last)|
gauss(range)|
instance_create_depth(x,y,depth,object)|
instance_create_depth_moving(x,y,depth,object,speed,direction,[gravity,[gravdir]])|
instance_create_depth_moving_ext(x,y,depth,object,speed,direction,[addhspeed,addvspeed,[gravity,[gravdir]]])|
instance_create_moving(x,y,object,speed,direction,[gravity,[gravdir]])|
instance_create_moving_ext(x,y,object,speed,direction,[addhspeed,addvspeed,[gravity,[gravdir]]])|
instance_destroy_id(obj)|
instance_destroy_other()|
instance_nearest_notme(x,y,obj)|
instance_some(obj)|
irandom_fresh(oldval,min,max):val|randomizes an integer within supplied range without repeating current value#
make_color_hsv_standard(hue 0-360,sat 0-100,val 0-100):color|
merge_color_corrected(col1,col2,factor)|
mouse_back_button()|
mouse_check_direct()|
mouse_forward_button()|
mouse_in_window()|
move_towards_gravity(xto,yto,gravity)|
object_is_child_of(object)|
object_other_is_child_of(object)|
outside_room()|workaround for instances without a sprite#
pick(which,opt1,opt2,...)|
point_line_lerp(px,py,x1,y1,x2,y2,segment)|Returns the projected value from the given point to the given line.#px,py       point to measuring from#x1,y1       1st end point of line#x2,y2       2nd end point of line#segment     set to true to limit to the line segment#/ GMLscripts.com/license#
registry_read_dword(addr,default)|
registry_write_dword(addr,val)|
str_cat(val1,val2,...)|
str_ins(str%ing,val1,val2...)|
string_better(real)|string(1.012562536) = "1.01"#string_better(1.012562536) = "1.01256254"#
string_number(string)|
string_ord_at(str,pos)|
string_pad(number,digits)|
url_open(url)|if (!string_pos("http:",argument0) && !string_pos("https://",argument0)) execute_shell("http://"+argument0,"")#
variable_instance_exists(inst,var)|
variable_instance_get(inst,var)|
window_has_focus()|
window_is_maximized()|
window_is_minimized()|
window_maximize()|
window_minimize()|